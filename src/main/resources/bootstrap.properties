# default name
spring.application.name=jweb-default

# default config name
spring.config.name=jweb

# default port
server.port=8805

# default close
eureka.client.enabled=false
spring.cloud.config.enabled=false
